@model IEnumerable<Lemon.Models.Fruit>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Soil)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Feature)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Water)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Soil)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Feature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Water)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FruitID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FruitID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FruitID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
